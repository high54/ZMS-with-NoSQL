import { NgZone } from '@angular/core';
import { services } from 'zetapush-js';
import 'rxjs/add/operator/publish';
import { ZetaPushClient } from './core';
export interface ApiError {
    code: string;
    location: string;
    message: string;
}
export declare class Api extends services.Macro {
    $getUserId(): string;
}
export declare function createApi(client: ZetaPushClient, zone: NgZone, Api: any): Api;
