import { SmartClient, ClientOptions } from 'zetapush-js';
export declare class ZetaPushClient extends SmartClient {
    constructor(options: ClientOptions);
    getDelegateToken(): string;
}
export declare class ZetaPushConnection {
    private client;
    constructor(client: ZetaPushClient);
    disconnect(): Promise<void>;
    connect(credentials?: any): Promise<void>;
}
